# 千岸集成验证报告

> 生成时间：自动初始化（待实际测试后填入具体数据）

---

## 1. 环境信息

| 组件 | 版本 | 备注 |
|------|------|------|
| Python | `<!-- python --version -->` | 后端运行时 |
| Node.js | `<!-- node -v -->` | 前端运行时 |
| Uvicorn | `<!-- uvicorn --version -->` | ASGI 服务器 |
| QIANAN_MOCK | `<!-- echo $QIANAN_MOCK -->` | 1 = Mock 模式，0 = 真实百炼 API |
| BAILIAN_API_KEY | `<!-- test -n $$BAILIAN_API_KEY && echo "configured" || echo "missing" -->` | 百炼密钥状态 |

---

## 2. 后端 API 验证

### 2.1 必选端点（不能 FAIL）

| 端点 | 方法 | 期望 | 实际 | 状态 |
|------|------|------|------|------|
| `/api/health` | GET | 200, `{status: "ok"}` | | ⬜ |
| `/api/rules` | GET | 200, 5 平台规则 | | ⬜ |
| `/api/ideation` | POST | 200, ≥1 条建议 | | ⬜ |
| `/api/economics` | POST | 200, 5 平台测算 | | ⬜ |
| `/api/audit` | POST | 200, 合规结果 | | ⬜ |
| `/api/generate` | POST | 200, task_id | | ⬜ |
| `/api/tasks/{id}` | GET | 200, 轮询至 done/failed | | ⬜ |
| `/api/admin/stats` | GET | 200, 统计数据 | | ⬜ |
| `/api/skills/registry` | GET | 200, 技能目录 | | ⬜ |
| `/api/feedback` | POST | 200, 反馈接收 | | ⬜ |
| `/api/files` | GET | 200, 文件列表 | | ⬜ |
| `/api/trends` | GET | 200, 热搜词 | | ⬜ |

### 2.2 Pipeline 详细输出

```
<!-- smoke test 实际输出粘贴于此 -->
```

### 2.3 已知问题

| 问题 | 严重度 | 状态 |
|------|--------|------|
| `server/rules/` 目录不存在 | 🔴 阻塞 | 未修复 |
| 规则 JSON 仅 Amazon 骨架 | 🔴 阻塞 | 未修复 |
| Pipeline 因缺规则库启动失败 | 🔴 阻塞 | 未修复 |
| | | |

---

## 3. 百炼 API 验证

> 运行 `cd qianan && python scripts/smoke_test_bailian.py` 获取实际结果。

| 测试项 | 模型 | 必选 | 期望 | 实际 | 状态 |
|--------|------|------|------|------|------|
| 文本生成 | Qwen-Max | ✅ | 返回非空文本 | | ⬜ |
| 图片生成 | Wanx / qwen-image | ✅ | 返回 HTTP 图片 URL | | ⬜ |
| 视觉理解 | Qwen-VL | ⬜ | 返回商品描述 | | ⬜ |

### 3.1 详情

```
<!-- smoke_test_bailian.py 实际输出粘贴于此 -->
```

---

## 4. 前端验证

### 4.1 构建

| 检查项 | 命令 | 期望 | 实际 | 状态 |
|--------|------|------|------|------|
| npm install | `cd qianan/web && npm install` | 0 错误 | | ⬜ |
| TypeScript 类型检查 | `npx tsc --noEmit` | 0 类型错误 | | ⬜ |
| Dev server 启动 | `npm run dev` | localhost:3000 响应 | | ⬜ |
| 首页加载 | 浏览器访问 `/` | 200, 含输入表单 | | ⬜ |
| API 连通 | 调用 `/api/health` | 200 | | ⬜ |

### 4.2 页面清单

| 页面 | 路由 | 功能 | 状态 |
|------|------|------|------|
| 首页 + 输入 | `/` | 上传图 + 卖点 + 选平台 → 生成任务 | ⬜ |
| 工作台 | `/workbench` | 选品 / 生成进度 / 利润测算 / 文件管理 | ⬜ |
| 结果页 | `/result/[taskId]` | 5 台差异 / 合规报告 / 导出 / 上架 | ⬜ |
| 规则库 | `/rules` | 结构化规则透明展示 | ⬜ |
| 文件管理 | `/files` | 已生成包浏览 / 下载 / 删除 | ⬜ |
| Agent 中心 | `/agent` | 记忆 / 技能 / 进化提案 | ⬜ |
| Copilot | `/copilot` | · | ⬜ |
| 管理后台 | `/admin` | 运营统计 / 任务管理 | ⬜ |

---

## 5. 端到端 验证

### 5.1 演示脚本

```bash
# 1. 启动后端
cd qianan/server && QIANAN_MOCK=1 uvicorn app.main:app --port 8000 &

# 2. 前端
cd qianan/web && npm run dev &

# 3. 跑 smoke test
cd qianan/server && python scripts/smoke_test.py

# 4. 跑百炼冒烟
cd qianan && python scripts/smoke_test_bailian.py
```

### 5.2 验收标记

```
<!-- 跑通后勾选 -->
[ ] 上传一张白底图 + 中文卖点
[ ] 30 秒内看到 5 平台 × 5 语言上架包
[ ] 合规报告通过（或自愈后通过）
[ ] 导出 CSV / 图片包可下载
[ ] 演示视频录制完成（3 分钟）
[ ] 部署 URL 可访问
```

---

## 6. 烟雾测试脚本索引

| 脚本 | 路径 | 用途 |
|------|------|------|
| 后端 Smoke Test | `qianan/server/scripts/smoke_test.py` | 验证 11 个 API 端点 + Mock pipeline |
| 百炼冒烟测试 | `qianan/scripts/smoke_test_bailian.py` | 验证文本/图片/视觉 API 可用性 |
| 前端类型检查 | `qianan/web/scripts/check-types.sh` | TypeScript 编译检查 |
| 一键验证 | `make verify` | 三项同时跑 |

---

## 7. 下一步行动

1. **创建 `server/rules/` 目录并补全 5 平台规则 JSON**（Amazon/Shopee/AliExpress/Lazada/TikTok Shop）
2. **完成前端 `src/` 目录与 API 连接层**（目前 `qianan/web/` 缺 `src/` 结构）
3. **配置百炼 API Key**（填入 `server/.env`，切换 QIANAN_MOCK=0 实测）
4. **准备 3 个演示样例商品**（覆盖不同类目）
5. **部署上线**（阿里云函数计算 / Vercel）
6. **录制演示视频**（3 分钟端到端走通）
